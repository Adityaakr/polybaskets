## The **bet-token** program

Separate FT contract for the BET lane in Polybaskets.

This workspace contains:
- `bet-token` binary crate
- `bet-token-app` business logic
- `bet-token-client` generated Rust client

### Building

```bash
cargo build --release
```

### Testing

```bash
cargo test
```
